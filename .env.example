# Auth — hardcoded credentials (swap for real auth later)
AUTH_USERNAME=admin
AUTH_PASSWORD=change-me
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Supabase — direct Postgres connection (transaction mode pooler)
# Find under: Supabase Dashboard → Settings → Database → Connection string → Transaction mode
DATABASE_URL=postgres://postgres.kfztolwstnfwooifplrw:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres

# Hookdeck
HOOKDECK_API_KEY=

# Make
MAKE_API_TOKEN=
MAKE_TEAM_ID=                  # numeric team/org ID
NEXT_PUBLIC_MAKE_ORG_ID=       # your Make org slug (e.g. "vdt-homes") for scenario edit URLs

# n8n
N8N_BASE_URL=https://your-n8n-instance.com/api/v1
N8N_API_KEY=
N8N_INSTANCE_URL=https://your-n8n-instance.com        # server-side (API base for edit links fallback)
NEXT_PUBLIC_N8N_INSTANCE_URL=https://your-n8n-instance.com  # client-side edit links
